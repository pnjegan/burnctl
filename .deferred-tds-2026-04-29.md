# Deferred TDs from 2026-04-29 dashboard smoke test

These were identified during tonight's smoke test but not filed 
in the browser-focused batch (TD-15..TD-18, commit 934ef09). 
Will be promoted to TD-19 onward when filed tomorrow.

## Pending

1. **WoW total +8330% — F4 zero-floor verification target.**
   Daily brief shows mathematically valid but practically 
   nonsense number due to near-zero baseline divisions ($7.14 
   day, $5.39 day in 7d window). F4 fixes by design.
   Priority: P1 (F4-related, defer until F4 lands).

2. **TOP ACTIONS dollar string duplicated.**
   "$4170.91/mo (~$4170.91/mo)" — daily_report.py:_recommendations_section 
   appends $ that's already in the message. One-line fix.
   Priority: P2.

3. **TOP ACTIONS vs Insights ranking divergence.**
   Same visual weight, different sort orders, no labels 
   distinguishing "highest-value action" from "most recent."
   Priority: P3.

4. **Generate Fix button rendered inconsistently.**
   Whitelist narrower than fix_tracker.py template inventory. 
   Some compactions/spikes/repeated_reads get the button, others 
   don't.
   Priority: P3.

5. **Fix tracker shows no fixes applied in 11 days.**
   Either flow broken or fixes happening outside tracker 
   awareness. Sanity check: DB activity + CLAUDE.md mtimes.
   Priority: P3.

6. **Stably-worsened fix #12 (WikiLoop) not auto-flagged.**
   F2/F3 verified compute_delta deterministic today, so the 
   worsening is real, not noise. fix_regressing rule may exist 
   per CHANGELOG L1350 — verify it fires; if so, surface more 
   prominently.
   Priority: P3.
